# Password Generator 🔐

A clean, responsive password generator built with ReactJS, HTML & CSS.  
Customize your password using lowercase/uppercase letters, digits, special characters, optional spacing, a disguise mode, and a strength meter simultaneously! 🧡🖤

## Features ✨

- Generate random passwords based on:
  - Lowercase letters (a–z)
  - Uppercase letters (A–Z)
  - Digits (0–9)
  - Special characters (@$!%*?&#)
  - Spacing (_)
- Adjustable password length via range slider
- Password strength indicator using simple regex-based scoring
- Disguise mode to mask the real password
- One-click copy to clipboard
- Accessible status messages (using `aria-live="polite"`)

---

## Tech Stack 🛠

- **React**
- **JavaScript (ES6+)**
- **CSS** (custom styling)
- **Vite**

---

## Getting Started 🚀

Clone the repository:

```bash
git clone https://github.com/AaX003/pass-generator.git
cd pass-generator
```

## Install Dependencies 🧰
```bash
npm install
npm run dev # vite
npm start # CRA
```

### Run Development Server 🧠
```bash
http://localhost:5173 # vite

http://localhost:3000 # CRA
```
